import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================
  // BRAND COLORS
  // =========================

  static const primary = Color(0xFF18E278);
  static const secondary = Color(0xFF0A0A0A);

  // =========================
  // STATUS COLORS
  // =========================

  static const success = Color(0xFF18E278);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // =========================
  // LIGHT THEME
  // =========================

  static const lightBackground = Color(0xFFF8F8F8);
  static const lightSurface = Color(0xFFFFFFFF);

  static const lightText = Color(0xFF0A0A0A);
  static const lightSecondaryText = Color(0xFF6B7280);

  static const lightBorder = Color(0xFFE5E7EB);

  // =========================
  // DARK THEME
  // =========================

  static const darkBackground = Color(0xFF0A0A0A);
  static const darkSurface = Color(0xFF151515);

  static const darkText = Color(0xFFFFFFFF);
  static const darkSecondaryText = Color(0xFFA1A1AA);

  static const darkBorder = Color(0xFF272727);

  // =========================
  // RIDE / MAP
  // =========================

  static const pickup = Color(0xFF18E278);
  static const destination = Color(0xFFEF4444);
  static const route = Color(0xFF18E278);

  static const driverOnline = Color(0xFF18E278);
  static const driverOffline = Color(0xFF6B7280);
}
